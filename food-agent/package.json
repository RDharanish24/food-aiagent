{
  "name": "food-agent-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && python -m pip install -r requirements.txt && cd ../frontend && npm install",
    "install:browser": "cd backend && python -m playwright install chromium",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run install:all && npm run install:browser"
  }
}
